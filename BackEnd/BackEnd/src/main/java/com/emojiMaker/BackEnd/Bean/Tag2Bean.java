package com.emojiMaker.BackEnd.Bean;

import org.springframework.stereotype.Component;

@Component
public class Tag2Bean {

    /* TODO 도건 Server에서 request_id, tag_name, emoji_url, emoji_tag 날라옴
        1. Status 조회
        2. Status 수정
        3. DAO 매핑 -> 저장
        # DAO : Id, request_id, emoji_url, tag_name, emoji_tag */
}
